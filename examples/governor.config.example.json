{
  "version": 1,
  "profiles": {
    "echo-test": {
      "runner": "echo",
      "description": "Safe builtin echo runner for preview/execute smoke tests",
      "timeout": 300,
      "enabled": true
    },
    "fake-validator": {
      "runner": "command",
      "description": "Local fake validator (scripts/fake_agent.py) — adjust path for your repo",
      "argv": ["python", "scripts/fake_agent.py"],
      "timeout": 300,
      "enabled": true
    },
    "cursor-local": {
      "runner": "command",
      "description": "Legacy placeholder — prefer cursor-headless-local.",
      "argv": [],
      "timeout": 900,
      "enabled": false
    },
    "cursor-headless-local": {
      "runner": "command",
      "description": "Cursor Headless CLI executor. Fill argv after local verification.",
      "argv": [],
      "timeout": 1800,
      "enabled": false
    },
    "cursor-governor-auto": {
      "runner": "command",
      "description": "Cursor Headless ask/read-only Governor provider (model auto). Fill argv locally.",
      "argv": [],
      "timeout": 900,
      "enabled": false
    },
    "chatbang-local": {
      "runner": "command",
      "description": "Not for executor — use: governor advisor ask --provider chatbang",
      "argv": [],
      "timeout": 900,
      "enabled": false
    },
    "claude-local": {
      "runner": "command",
      "description": "Claude Code -p (prompt on stdin). Enable only when auth works; no secrets in argv.",
      "argv": ["claude", "-p", "--output-format", "text"],
      "timeout": 900,
      "enabled": false
    }
  }
}
